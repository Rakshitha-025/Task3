import React from 'react';
import WordCounter from './components/WordCounter';
import './App.css'; // Assuming WordCounter component is in a separate file

const App = () => {
  return (
    <div className="App">
      <WordCounter />
    </div>
  );
};

export default App;

